generator client {
    provider = "prisma-client-js"
}

datasource db {
    provider = "postgresql"
    url      = env("DATABASE_URL")
}

model Attempt {
    id        String   @id @default(uuid())
    userId    String
    date      DateTime @default(now())
    correct   Int
    incorrect Int
    total     Int
}
